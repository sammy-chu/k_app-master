# 市场监控前端页面功能说明 (Market Monitor Frontend Features)

本文档整理了 `market-monitor` 项目中前端页面的股票监测功能、交互设计及底层数据联动逻辑。

## 1. 核心监控页面矩阵

目前系统包含以下几个核心的股票监控页面，分别对应不同的量化与盘口交易场景：

### 1.1 震荡突破监控 (Breakout) & 小区间震荡监控 (Range)
- **定位**：基于 K 线技术形态的分钟级策略扫描。Breakout 寻找极度压缩后的突破机会；Range 寻找宽幅震荡的高抛低吸机会。
- **数据源**：后端直接抓取 `minute_indicators` 表的特征数据，在 Node.js 内存中进行 A~H 8项核心指标的实时打分（In-Memory Evaluation）。
- **页面特色功能**：
  - **满分严格过滤**：默认仅展示综合评分（Score）为 100 分的标的。
  - **时间倒序呈现**：抓取今日全量历史触发的满分记录，按时间倒序排列，最多展示 50 条，确保第一时间看到最新机会。
  - **条件矩阵可视化**：A~H 条件的实际计算值直接在表格中平铺展示，并根据是否达标动态标绿（✅）或标红（❌）。
  - **单股历史诊断 (Timeline)**：页面顶部提供专属单股查询入口，输入代码可查阅该股票今日的分钟级“体检报告”，以垂直时间轴展示各个时间点分数和指标变动。

### 1.2 活跃主买监控 (Active Trading)
- **定位**：盘口高频交易（Tape Reading）特征捕捉，监测短时间内的资金异动。
- **数据源**：`market_data.active_trading_symbols`（高频成交）与 `market_data.iceberg_symbols`（冰山连击）。
- **页面特色功能**：
  - **冰山爆单闪烁置顶 (最高优先级)**：当底层识别到“2秒内同一股数连击超过3次”时，前端会对该股票进行最高优先级置顶，并带有醒目的紫红色背景与呼吸闪烁动画（持续5秒），附带 `⚡ 100股×4连击` 专属徽标。
  - **高频宽限期置顶 (次级优先级)**：对于短时间成交次数 ≥ 10 次的股票，即使掉出活跃榜单，前端也会提供 5 分钟的“置顶保留期”，以黄橙色背景显示。

### 1.3 边界预警 (Boundary Alerts)
- **定位**：关键支撑位与阻力位的价格触碰预警。
- **功能特点**：展示距离上轨或下轨最近的股票，提供多维度的触发类型徽标（如 `4A`、`4B-UP` 等）。

---

## 2. 全局通用交互与过滤系统

为了满足高频交易员“零延迟、所见即所得”的需求，所有监控页面均共享以下底层交互机制：

### 2.1 全局独立屏蔽系统 (Block System)
- **跨页面共享**：基于浏览器的 `localStorage`（键名 `sharedBlockedStocks`），在一个页面屏蔽某只股票，所有监控页面同步生效。
- **多维度极速交互**：
  - **行内快捷按钮**：鼠标悬停在表格“代码”列时，代码右侧自动浮现 `×` 按钮，点击秒级屏蔽。
  - **右键菜单屏蔽**：在任意数据行上点击右键，呼出自定义上下文菜单「🚫 屏蔽此股票」。
  - **集中管理弹窗**：页面右上角「🚫 屏蔽管理」按钮，点击弹出独立 Modal，支持逗号分隔批量添加、列表式解封以及“一键清空”。

### 2.2 折叠式零延迟过滤面板 (Filter Panel)
- **折叠交互**：支持展开/收起的面板设计，节省表格可视空间。折叠状态下会通过胶囊标签（Pills）回显当前生效的过滤条件。
- **内存级过滤**：前端通过全局变量（如 `allItems`）缓存后端下发的全量数据。修改价格区间（Price）、今日成交量（Volume）等条件后，使用 JS 数组 `filter()` 进行内存过滤并重绘 DOM，**无需重新请求后端 API**，实现 0 延迟响应。

### 2.3 状态感知与轮询 (Auto-Refresh)
- **心跳机制**：页面默认每 10~15 秒向后端 API 轮询一次最新快照数据。
- **视觉心跳**：标题栏右上角配有状态指示灯（Status Dot），正常拉取时显示为呼吸绿灯，断网或接口报错时变为红灯并提示“等待数据”。

---

## 3. 技术架构总结
系统采用了 **“后端重计算聚合 + 前端重交互缓存”** 的架构：
1. **Node.js 后端** 负责处理复杂的表连接（如聚合实时成交量 `ohlc_snapshot` 和冰山记忆表 `iceberg_symbols`）以及策略的内存公式验证。
2. **Vanilla JS 前端** 利用 DOM 原生操作和纯内存数据过滤，确保了在大批量股票高频刷新下的渲染性能与交互丝滑度。