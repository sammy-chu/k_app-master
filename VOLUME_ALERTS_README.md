# 成交量监控系统 (Volume Alerts) 使用说明

本文档旨在帮助用户理解 **成交量提醒 (Volume Alerts)** 页面的各项指标含义、参数配置及使用方法。

## 1. 系统简介

该页面用于基于 **时间节点 (Time Checkpoints)** 监控全市场股票的成交量进度。系统不再使用单一的“放量/缩量”阈值，而是根据预设的时间点（如开盘2小时、收盘前等），检查股票的累计成交量是否达到预期进度。

**核心逻辑**：
系统会在指定的时间点（如开盘后120分钟），检查股票当前的成交量是否达到了历史日均量的一定比例（如30%）。如果**实际完成比例高于或等于预期**，系统将发出提醒，帮助交易者发现成交量**达标或放量**的标的。

---

## 2. 核心机制：时间检查点 (Time Checkpoints)

系统通过“检查点”来管理监控逻辑。每个检查点包含三个要素：

1.  **时间 (Time)**：开盘后经过的分钟数。
    *   **基准时间**：北京时间 08:00 (美股夜盘/盘前时段开始)
    *   **计算公式**：当前北京时间分钟数 - 480 (08:00)
    *   **示例**：
        *   北京时间 09:00 -> 60分钟
        *   北京时间 10:00 -> 120分钟
        *   北京时间 16:00 -> 480分钟 (收盘)
2.  **预期进度 (Expected %)**：在该时间点，股票应该完成全天均量的百分比（例如 `30%`）。
3.  **标签 (Label)**：该检查点的名称（例如 `开市2小时`）。

**提醒触发条件**：
$$ \text{实际量比} \ge \text{预期进度} $$
*(注：实际量比 = 今日当前累计量 / 20日历史截尾均量)*

**示例**：
- 设定检查点：`120分钟`，预期 `30%`。
- 某股票在开盘120分钟时，实际量比达到 `35%`。
- **触发提醒**：超出 `5%` (35% - 30%)，提示“开市2小时，已达日均量的 35% (目标 30%) ✓”。

---

## 3. 告警列表指标说明

在提醒列表中，您会看到以下关键数据：

### **超出 (Excess)**
- **定义**：实际进度超过预期进度的百分比。
- **计算**：$$ \text{超出} = \text{实际百分比} - \text{预期百分比} $$
- **含义**：数值越大，表示成交量越活跃，超出预期越多。

### **实际 vs 预期 (Actual vs Expected)**
- **实际**：今日当前成交量占历史日均量的比例。
- **预期**：当前检查点设定的目标比例。

### **当前量 (Current Volume)**
- **定义**：今日截至目前的累计成交量。

---

## 4. 参数配置指南

点击页面顶部的 **"⚙️ 系统参数配置"** 面板，可以自定义检查点：

### **时间检查点配置 (Time Checkpoints)**
您可以在表格中自由添加、删除检查点：
- **时间 (分钟)**：输入距离开盘（09:30 ET）的分钟数。
    - 60 = 10:30
    - 120 = 11:30
    - 390 = 16:00 (收盘)
- **预期完成 %**：输入小数（如 `0.3` 代表 30%）。
- **标签**：自定义显示的名称。

### **其他参数**
- **计算均量的历史天数 (Volume History Days)**：默认为 `20`，用于计算基准日均量（采用截尾平均法，剔除最高/最低10%）。

**操作提示**：
*   修改配置后，务必点击 **"保存配置"** 按钮，系统会即时更新后台逻辑。
*   点击 **"立即扫描"** 可强制系统在后台立即执行一次检查（主要用于调试，正常情况下系统每5分钟自动检查一次）。

---

## 5. 列表筛选功能

页面下方的 **"筛选配置"** 用于过滤显示结果：

*   **最小差距百分比 (Threshold)**：
    - 例如设为 `0.1` (10%)。
    - 列表将只显示“实际进度比预期低 10% 以上”的股票。
    - 过滤掉那些只差 1-2% 的轻微波动。
*   **启用筛选**：勾选后上述阈值生效；取消勾选则显示所有触发的数据。

---

## 6. 数据导出

如果您需要导出历史数据进行分析：
- 请使用 API `/api/volume/summary?start=YYYY-MM-DD&end=YYYY-MM-DD&format=csv` 直接下载 CSV 文件。
