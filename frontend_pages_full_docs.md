# Market Monitor 前端页面全景功能文档 (Frontend Pages Documentation)

本项目 (`market-monitor`) 的前端架构由多个专注特定交易场景的独立页面组成。这些页面通过纯前端（Vanilla JS）实现高频渲染，并与 Node.js API 紧密配合。

以下是项目中**所有前端页面**的功能分类与详细整理。

---

## 1. 首页与大盘中枢 (Dashboard & Overview)

### 1.1 首页控制台 `index.html`
- **定位**：整个监控系统的入口导航与核心指标总览大屏。
- **功能特色**：
  - **全局导航中心**：提供前往所有子扫描器、预警页面的快捷入口。
  - **大盘状态速览**：通常用于展示全市场的宏观状态（如上涨/下跌家数比、全市场成交额分布等）。
  - **系统状态监控**：展示后端服务连接状态、数据延迟等底层健康指标。

### 1.2 市场核心排序 `ranking.html`
- **定位**：全市场活跃度与流动性风向标。
- **功能特色**：
  - **多维排行榜**：实时展示全市场的**成交量榜 (Volume)**、**成交额榜 (Turnover)**、**涨跌幅榜**。
  - **流动性过滤**：帮助交易员快速过滤掉死水股，锁定当天资金最扎堆的核心标的。
  - **动态刷新**：基于内存缓存和表格局部更新，实现榜单的丝滑跳动。

---

## 2. 核心量化扫描器 (Screener Series)
> **通用特性**：所有 Screener 页面均配备 0 延迟折叠筛选面板（价格区间、今日成交量）、跨页面共享屏蔽系统（右键/行内快捷屏蔽）以及单股 Timeline 诊断功能。

### 2.1 震荡突破监控 `screener-breakout.html`
- **逻辑模型**：捕捉价格在经历极度压缩（ATR/HV收缩）后即将向上或向下爆发的标的。
- **扫描/判定条件 (A-H矩阵)**：
  - **A. ATR Ratio < 0.5**：短期波动远低于中期波动（极度压缩）。
  - **B. Day ATR% > 0.02**：日线级别波动足够大（有突破空间）。
  - **C. ATR Stab < 1.3**：ATR10未出现异常放大（仍在压缩状态）。
  - **D. |Slope| < 0.0025**：价格重心近乎水平（无明确方向）。
  - **E. HV Ratio < 0.5**：短期波动率远低于历史均值（极度平静）。
  - **F. 0.3 ≤ VOL Ratio ≤ 0.7**：成交量处于缩量状态（蓄势）。
  - **G. Touch (a≥2, b≥2, a+b≥5)**：价格在区间上下边缘均有触碰（区间确认）。
  - **H. Up≥6, Dn≥6**：中长期价格均匀分布（非单边）。
- **展示特色**：默认强制展示综合评分（Score）为 **100 分**的完美形态，按时间倒序展示。

### 2.2 小区间震荡监控 `screener-range.html`
- **逻辑模型**：捕捉价格在明确箱体区间内有序波动，适合高抛低吸策略的标的。
- **扫描/判定条件 (A-H矩阵)**：
  - **A. 0.3 ≤ ATR Ratio ≤ 1.2**：短期与中期波动相对均衡。
  - **B. Day ATR% > 0.02**：日线波动足够（交易活跃）。
  - **C. ATR Expansion < 1.3**：短期波动未过度放大。
  - **D. Slope Bias ≤ 20%**：价格重心偏移在区间20%以内。
  - **E. HV Ratio < 0.7**：短期波动率低于历史均值（相对平静）。
  - **F. 0.35 ≤ VOL Ratio ≤ 1.3**：成交量相对均衡（温和换手）。
  - **G. Touch (a≥2 or b≥2)**：价格至少触碰一次上下虚线之一。
  - **H. Up≥6, Dn≥6**：中长期价格均匀分布。
- **展示特色**：同样强制过滤展示 100 分完美标的，重点关注箱体上下轨触碰次数以及重心偏移率（Slope Bias）。

### 2.3 稳健型趋势监控 `screener-stable.html`
- **逻辑模型**：关注趋势极其平滑、波动极小、沿着均线稳步推升（或下跌）的标的（类似“推土机”形态）。
- **扫描/判定条件**：
  - **高 R² (R-Squared)**：价格对均线或线性回归线的拟合优度极高（通常 > 0.85）。
  - **平缓的 Slope**：线性回归斜率保持在一个稳定且不陡峭的区间。
  - **极低的波动率**：ATR/价格的比例极小，日内无明显尖刺（Spikes）。

### 2.4 动量/震荡指标监控 `screener-oscillator.html` & `swing-screener.html`
- **逻辑模型**：基于传统动量指标或波段波谷（Swing High/Low）构建的扫描器。
- **扫描/判定条件**：
  - **超买超卖回归**：如 RSI < 30 后拐头向上，或 Stochastic %K 穿越 %D。
  - **结构突破**：最新价格突破最近的 Swing High，或跌破 Swing Low。
  - **MACD 背离/金叉**：MACD 柱状图翻红或快慢线交叉。

### 2.5 综合扫描器底座 `screener.html`
- **逻辑模型**：提供给用户自定义组合条件的通用筛选沙盒。
- **扫描/判定条件**：支持用户在页面上自由组合基础量价指标（如 Price > X, Vol > Y, Change% > Z 等）。

---

## 3. 盘口与资金异动 (Order Flow & Tape Reading)

### 3.1 活跃主买与冰山单监控 `active-trading.html`
- **逻辑模型**：盘口微观流动性与高频刷单捕捉。
- **扫描/判定条件**：
  - **常规活跃警报**：1 分钟内同一股票的成交笔数（Trade Count）大于 3 次。
  - **冰山连击警报**：2 秒内，出现同一股数（Iceberg Size）密集连击超过 3 次。
- **展示特色**：
  - 冰山爆单获得最高优先级置顶，附带紫红色背景呼吸闪烁与专属徽标（如 `⚡ 100股×4连击`）。
  - 常规活跃榜单掉出后享有 5 分钟的黄橙色“置顶保留期”。

### 3.2 大单监控 `large-orders.html`
- **逻辑模型**：追踪主力资金的单笔重注。
- **扫描/判定条件**：
  - **单笔名义价值**：单笔成交金额（Price × Size）≥ $100,000（或用户设定阈值）。
  - **单笔股数**：单笔成交股数异常巨大（如单笔 ≥ 10,000 股）。

### 3.3 异常交易与L2异动 `abnormal-trades.html` & `l2_alert_history.html`
- **逻辑模型**：Level 2 订单簿深度异常监测。
- **扫描/判定条件**：
  - **扫单 (Sweeping)**：一笔市价单瞬间吃透（穿越）3 档以上的买/卖盘深度。
  - **大单撤单 (Spoofing)**：买一/卖一档挂有远超均值的巨量限价单，且在价格逼近时瞬间撤销。

### 3.4 巨量飙升预警 `volume-surge.html` & `volume-alerts.html`
- **逻辑模型**：成交量突变捕捉。
- **扫描/判定条件**：
  - **相对均量飙升**：当前 1 分钟的成交量 ≥ 过去 15 分钟/60 分钟平均成交量的 N 倍（如 5倍、10倍）。
  - **绝对放量**：当前分钟成交量超过某个硬性阈值且伴随价格剧烈波动。

---

## 4. 价格形态与边界预警 (Technical Patterns & Boundaries)

### 4.1 边界触碰预警 `boundary-alerts.html`
- **逻辑模型**：监控价格逼近或突破关键支撑位/阻力位。
- **扫描/判定条件**：
  - 价格进入定义的上下界（Upper/Lower Boundary）的极小缓冲区内（例如距离边界 < 0.1%）。
  - 根据价格动能区分触发类型：
    - `4A`：缓慢靠近边界。
    - `4B-UP / 4B-DN`：带量急速刺穿边界。

### 4.2 山峰/波峰形态预警 `hills.html` & `hill-alerts.html`
- **逻辑模型**：捕捉日内价格快速拉升后回落形成的“倒V”型结构。
- **扫描/判定条件**：
  - 短时间内（如 5-10 分钟）价格急剧上涨超过 X%。
  - 随后立即出现回落，且回落幅度达到前一波上涨幅度的 50% 以上。
  - 顶部常伴随显著放大的成交量（量峰）。

### 4.3 波动率监控 `volatility.html`
- **逻辑模型**：捕捉期权交易员或突破交易者最关注的波动率极值。
- **扫描/判定条件**：
  - **波动率收敛**：日内真实波幅（TR）或 10周期 ATR 跌至历史低点分位数（如 < 10th Percentile）。
  - **波动率扩张**：短期 HV（历史波动率）突破长期 HV 均值并急剧发散。

---

## 5. 历史分析与图表展示 (History & Charting)

### 5.1 历史警报总览 `alerts.html`
- **定位**：全系统预警信号的聚合与回溯。
- **功能特色**：以时间流的形式展示所有扫描器和预警器产生的信号日志，方便盘后复盘。

### 5.2 成交量图表分析 `volume-chart.html`
- **定位**：可视化的量价关系分析工具。
- **功能特色**：
  - 集成 `lightweight-charts` 等图表库，提供带成交量柱状图的 K 线界面。
  - 可在图表上直观标记出产生 `volume-surge` 或 `active-trading` 异动的具体 K 线节点。

---

## 附：前端架构核心机制
* **心跳轮询 (Polling)**：绝大多数页面采用定时拉取 API（`setInterval`，通常 10-15s）机制，而非 WebSocket，以降低后端长连接压力，同时契合交易员的观察节奏。
* **DOM 批量重绘**：通过拼接 HTML 字符串再赋值 `innerHTML` 的方式更新表格，避免大量 DOM 节点操作带来的性能卡顿。
* **纯静态部署支持**：HTML/CSS/JS 完全分离，前端页面没有复杂的构建工具（Webpack/Vite）强依赖，修改文件即可实时生效，极度适合量化交易场景下的快速迭代。